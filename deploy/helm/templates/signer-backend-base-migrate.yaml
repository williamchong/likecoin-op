apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/component: {{ .Values.signerBackendBase.name }}
  name: {{ .Values.signerBackendBase.name }}-migrate
  namespace: {{ .Values.namespace }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
type: Opaque
stringData:
  DB_CONNECTION_STR: {{ .Values.signerBackendBase.env.DB_CONNECTION_STR | quote }}
---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app.kubernetes.io/component: {{ .Values.signerBackendBase.name }}
  name: {{ .Values.signerBackendBase.name }}-migrate
  namespace: {{ .Values.namespace }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
spec:
  template:
    spec:
      containers:
      - name: {{ .Values.signerBackendBase.name }}
        image: {{ .Values.signerBackendBase.container.imageRepository }}:{{ .Values.appVersion }}
        imagePullPolicy: {{ .Values.signerBackendBase.container.imagePullPolicy}}
        command: ['sql-migrate', 'up']
        envFrom:
          - secretRef:
              name: {{ .Values.signerBackendBase.name }}-migrate
        resources:
          {{- toYaml .Values.signerBackendBase.resources | nindent 12 }}
      restartPolicy: "{{ .Values.signerBackendBase.migrate.restartPolicy | default "Never" }}"
  backoffLimit: {{ .Values.signerBackendBase.migrate.backoffLimit | default 1 }}
