apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.signerBackendBase.name }}
  name: {{ .Values.signerBackendBase.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/component: {{ .Values.signerBackendBase.name }}
  ports:
  - protocol: TCP
    port: {{ .Values.signerBackendBase.service.port }}
    targetPort: {{ .Values.signerBackendBase.container.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: {{ .Values.signerBackendBase.name }}
  name: {{ .Values.signerBackendBase.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: {{ .Values.signerBackendBase.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: {{ .Values.signerBackendBase.name }}
    spec:
      restartPolicy: Always
      containers:
      - name: {{ .Values.signerBackendBase.name }}
        image: {{ .Values.signerBackendBase.container.imageRepository }}:{{ .Values.appVersion }}
        imagePullPolicy: {{ .Values.signerBackendBase.container.imagePullPolicy}}
        envFrom:
          - secretRef:
              name: {{ .Values.signerBackendBase.name }}
          - secretRef:
              name: signer-private-key
        ports:
        - containerPort: {{ .Values.signerBackendBase.container.port }}
        resources:
          {{- toYaml .Values.signerBackendBase.resources | nindent 12 }}
