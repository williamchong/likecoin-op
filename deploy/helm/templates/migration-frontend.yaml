apiVersion: v1
kind: Service
metadata:
  labels:
    app: likecoin-migration-frontend
  name: {{ .Values.likecoin.migration.frontend.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/component: likecoin-migration-frontend
  ports:
  - protocol: TCP
    port: {{ .Values.likecoin.migration.frontend.service.port }}
    targetPort: {{ .Values.likecoin.migration.frontend.container.port }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.likecoin.migration.frontend.name }}-config
data:
  config.json: |-
{{ .Files.Get .Values.likecoin.migration.frontend.configFilePath | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.likecoin.migration.frontend.name }}-nginx-config
data:
  nginx.conf: |-
{{ .Files.Get .Values.likecoin.migration.frontend.nginxConfigFilePath | indent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: {{ .Values.likecoin.migration.frontend.name }}
  name: {{ .Values.likecoin.migration.frontend.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: {{ .Values.likecoin.migration.frontend.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: {{ .Values.likecoin.migration.frontend.name }}
    spec:
      restartPolicy: Always
      containers:
      - name: {{ .Values.likecoin.migration.frontend.name }}
        image: {{ .Values.likecoin.migration.frontend.container.imageRepository }}:{{ .Values.appVersion }}
        imagePullPolicy: {{ .Values.likecoin.migration.frontend.container.imagePullPolicy}}
        ports:
        - containerPort: {{ .Values.likecoin.migration.frontend.container.port }}
        volumeMounts:
          - name: config
            mountPath: /usr/share/nginx/html/config.json
            subPath: config.json
          - name: nginx-config
            mountPath: /etc/nginx/nginx.conf
            subPath: nginx.conf
        resources:
          {{- toYaml .Values.likecoin.migration.frontend.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ .Values.likecoin.migration.frontend.name }}-config
        - name: nginx-config
          configMap:
            name: {{ .Values.likecoin.migration.frontend.name }}-nginx-config
