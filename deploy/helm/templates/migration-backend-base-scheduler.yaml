apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: {{ .Values.migrationBackendBase.scheduler.name }}
  name: {{ .Values.migrationBackendBase.scheduler.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: {{ .Values.migrationBackendBase.scheduler.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: {{ .Values.migrationBackendBase.scheduler.name }}
    spec:
      restartPolicy: Always
      containers:
      - name: {{ .Values.migrationBackendBase.scheduler.name }}
        image: {{ .Values.migrationBackendBase.container.imageRepository }}:{{ .Values.appVersion }}
        imagePullPolicy: {{ .Values.migrationBackendBase.container.imagePullPolicy}}
        command: ["like-migration-backend-worker-cli", "scheduler"]
        envFrom:
          - secretRef:
              name: {{ .Values.migrationBackendBase.name }}
        resources:
          {{- toYaml .Values.migrationBackendBase.scheduler.resources | nindent 12 }}
