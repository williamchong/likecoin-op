{{- if .Values.likecollectiveIndexer.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.likecollectiveIndexer.name }}-api
  name: {{ .Values.likecollectiveIndexer.name }}-api
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/component: {{ .Values.likecollectiveIndexer.name }}-api
  ports:
  - protocol: TCP
    port: {{ .Values.likecollectiveIndexer.api.service.port }}
    targetPort: {{ .Values.likecollectiveIndexer.api.container.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: {{ .Values.likecollectiveIndexer.name }}-api
  name: {{ .Values.likecollectiveIndexer.name }}-api
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: {{ .Values.likecollectiveIndexer.name }}-api
  template:
    metadata:
      labels:
        app.kubernetes.io/component: {{ .Values.likecollectiveIndexer.name }}-api
    spec:
      restartPolicy: Always
      containers:
      - name: {{ .Values.likecollectiveIndexer.name }}-api
        image: {{ .Values.likecollectiveIndexer.container.imageRepository }}:{{ .Values.appVersion }}
        imagePullPolicy: {{ .Values.likecollectiveIndexer.container.imagePullPolicy }}
        envFrom:
          - secretRef:
              name: {{ .Values.likecollectiveIndexer.name }}
        ports:
        - containerPort: {{ .Values.likecollectiveIndexer.api.container.port }}
        resources:
          {{- toYaml .Values.likecollectiveIndexer.api.resources | nindent 12 }}
{{- end }}