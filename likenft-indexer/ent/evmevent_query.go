// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"likenft-indexer/ent/evmevent"
	"likenft-indexer/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EVMEventQuery is the builder for querying EVMEvent entities.
type EVMEventQuery struct {
	config
	ctx        *QueryContext
	order      []evmevent.OrderOption
	inters     []Interceptor
	predicates []predicate.EVMEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EVMEventQuery builder.
func (eeq *EVMEventQuery) Where(ps ...predicate.EVMEvent) *EVMEventQuery {
	eeq.predicates = append(eeq.predicates, ps...)
	return eeq
}

// Limit the number of records to be returned by this query.
func (eeq *EVMEventQuery) Limit(limit int) *EVMEventQuery {
	eeq.ctx.Limit = &limit
	return eeq
}

// Offset to start from.
func (eeq *EVMEventQuery) Offset(offset int) *EVMEventQuery {
	eeq.ctx.Offset = &offset
	return eeq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eeq *EVMEventQuery) Unique(unique bool) *EVMEventQuery {
	eeq.ctx.Unique = &unique
	return eeq
}

// Order specifies how the records should be ordered.
func (eeq *EVMEventQuery) Order(o ...evmevent.OrderOption) *EVMEventQuery {
	eeq.order = append(eeq.order, o...)
	return eeq
}

// First returns the first EVMEvent entity from the query.
// Returns a *NotFoundError when no EVMEvent was found.
func (eeq *EVMEventQuery) First(ctx context.Context) (*EVMEvent, error) {
	nodes, err := eeq.Limit(1).All(setContextOp(ctx, eeq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{evmevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eeq *EVMEventQuery) FirstX(ctx context.Context) *EVMEvent {
	node, err := eeq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EVMEvent ID from the query.
// Returns a *NotFoundError when no EVMEvent ID was found.
func (eeq *EVMEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eeq.Limit(1).IDs(setContextOp(ctx, eeq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{evmevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eeq *EVMEventQuery) FirstIDX(ctx context.Context) int {
	id, err := eeq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EVMEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EVMEvent entity is found.
// Returns a *NotFoundError when no EVMEvent entities are found.
func (eeq *EVMEventQuery) Only(ctx context.Context) (*EVMEvent, error) {
	nodes, err := eeq.Limit(2).All(setContextOp(ctx, eeq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{evmevent.Label}
	default:
		return nil, &NotSingularError{evmevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eeq *EVMEventQuery) OnlyX(ctx context.Context) *EVMEvent {
	node, err := eeq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EVMEvent ID in the query.
// Returns a *NotSingularError when more than one EVMEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (eeq *EVMEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eeq.Limit(2).IDs(setContextOp(ctx, eeq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{evmevent.Label}
	default:
		err = &NotSingularError{evmevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eeq *EVMEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := eeq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EVMEvents.
func (eeq *EVMEventQuery) All(ctx context.Context) ([]*EVMEvent, error) {
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryAll)
	if err := eeq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EVMEvent, *EVMEventQuery]()
	return withInterceptors[[]*EVMEvent](ctx, eeq, qr, eeq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eeq *EVMEventQuery) AllX(ctx context.Context) []*EVMEvent {
	nodes, err := eeq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EVMEvent IDs.
func (eeq *EVMEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if eeq.ctx.Unique == nil && eeq.path != nil {
		eeq.Unique(true)
	}
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryIDs)
	if err = eeq.Select(evmevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eeq *EVMEventQuery) IDsX(ctx context.Context) []int {
	ids, err := eeq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eeq *EVMEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryCount)
	if err := eeq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eeq, querierCount[*EVMEventQuery](), eeq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eeq *EVMEventQuery) CountX(ctx context.Context) int {
	count, err := eeq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eeq *EVMEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryExist)
	switch _, err := eeq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eeq *EVMEventQuery) ExistX(ctx context.Context) bool {
	exist, err := eeq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EVMEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eeq *EVMEventQuery) Clone() *EVMEventQuery {
	if eeq == nil {
		return nil
	}
	return &EVMEventQuery{
		config:     eeq.config,
		ctx:        eeq.ctx.Clone(),
		order:      append([]evmevent.OrderOption{}, eeq.order...),
		inters:     append([]Interceptor{}, eeq.inters...),
		predicates: append([]predicate.EVMEvent{}, eeq.predicates...),
		// clone intermediate query.
		sql:  eeq.sql.Clone(),
		path: eeq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TransactionHash string `json:"transaction_hash,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EVMEvent.Query().
//		GroupBy(evmevent.FieldTransactionHash).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eeq *EVMEventQuery) GroupBy(field string, fields ...string) *EVMEventGroupBy {
	eeq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EVMEventGroupBy{build: eeq}
	grbuild.flds = &eeq.ctx.Fields
	grbuild.label = evmevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TransactionHash string `json:"transaction_hash,omitempty"`
//	}
//
//	client.EVMEvent.Query().
//		Select(evmevent.FieldTransactionHash).
//		Scan(ctx, &v)
func (eeq *EVMEventQuery) Select(fields ...string) *EVMEventSelect {
	eeq.ctx.Fields = append(eeq.ctx.Fields, fields...)
	sbuild := &EVMEventSelect{EVMEventQuery: eeq}
	sbuild.label = evmevent.Label
	sbuild.flds, sbuild.scan = &eeq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EVMEventSelect configured with the given aggregations.
func (eeq *EVMEventQuery) Aggregate(fns ...AggregateFunc) *EVMEventSelect {
	return eeq.Select().Aggregate(fns...)
}

func (eeq *EVMEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eeq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eeq); err != nil {
				return err
			}
		}
	}
	for _, f := range eeq.ctx.Fields {
		if !evmevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eeq.path != nil {
		prev, err := eeq.path(ctx)
		if err != nil {
			return err
		}
		eeq.sql = prev
	}
	return nil
}

func (eeq *EVMEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EVMEvent, error) {
	var (
		nodes = []*EVMEvent{}
		_spec = eeq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EVMEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EVMEvent{config: eeq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eeq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eeq *EVMEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eeq.querySpec()
	_spec.Node.Columns = eeq.ctx.Fields
	if len(eeq.ctx.Fields) > 0 {
		_spec.Unique = eeq.ctx.Unique != nil && *eeq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eeq.driver, _spec)
}

func (eeq *EVMEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(evmevent.Table, evmevent.Columns, sqlgraph.NewFieldSpec(evmevent.FieldID, field.TypeInt))
	_spec.From = eeq.sql
	if unique := eeq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eeq.path != nil {
		_spec.Unique = true
	}
	if fields := eeq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, evmevent.FieldID)
		for i := range fields {
			if fields[i] != evmevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eeq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eeq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eeq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eeq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eeq *EVMEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eeq.driver.Dialect())
	t1 := builder.Table(evmevent.Table)
	columns := eeq.ctx.Fields
	if len(columns) == 0 {
		columns = evmevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eeq.sql != nil {
		selector = eeq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eeq.ctx.Unique != nil && *eeq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eeq.predicates {
		p(selector)
	}
	for _, p := range eeq.order {
		p(selector)
	}
	if offset := eeq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eeq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EVMEventGroupBy is the group-by builder for EVMEvent entities.
type EVMEventGroupBy struct {
	selector
	build *EVMEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eegb *EVMEventGroupBy) Aggregate(fns ...AggregateFunc) *EVMEventGroupBy {
	eegb.fns = append(eegb.fns, fns...)
	return eegb
}

// Scan applies the selector query and scans the result into the given value.
func (eegb *EVMEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eegb.build.ctx, ent.OpQueryGroupBy)
	if err := eegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EVMEventQuery, *EVMEventGroupBy](ctx, eegb.build, eegb, eegb.build.inters, v)
}

func (eegb *EVMEventGroupBy) sqlScan(ctx context.Context, root *EVMEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eegb.fns))
	for _, fn := range eegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eegb.flds)+len(eegb.fns))
		for _, f := range *eegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EVMEventSelect is the builder for selecting fields of EVMEvent entities.
type EVMEventSelect struct {
	*EVMEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ees *EVMEventSelect) Aggregate(fns ...AggregateFunc) *EVMEventSelect {
	ees.fns = append(ees.fns, fns...)
	return ees
}

// Scan applies the selector query and scans the result into the given value.
func (ees *EVMEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ees.ctx, ent.OpQuerySelect)
	if err := ees.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EVMEventQuery, *EVMEventSelect](ctx, ees.EVMEventQuery, ees, ees.inters, v)
}

func (ees *EVMEventSelect) sqlScan(ctx context.Context, root *EVMEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ees.fns))
	for _, fn := range ees.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ees.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ees.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
