// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"likenft-indexer/ent/likeprotocol"
	"likenft-indexer/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LikeProtocolQuery is the builder for querying LikeProtocol entities.
type LikeProtocolQuery struct {
	config
	ctx        *QueryContext
	order      []likeprotocol.OrderOption
	inters     []Interceptor
	predicates []predicate.LikeProtocol
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LikeProtocolQuery builder.
func (lpq *LikeProtocolQuery) Where(ps ...predicate.LikeProtocol) *LikeProtocolQuery {
	lpq.predicates = append(lpq.predicates, ps...)
	return lpq
}

// Limit the number of records to be returned by this query.
func (lpq *LikeProtocolQuery) Limit(limit int) *LikeProtocolQuery {
	lpq.ctx.Limit = &limit
	return lpq
}

// Offset to start from.
func (lpq *LikeProtocolQuery) Offset(offset int) *LikeProtocolQuery {
	lpq.ctx.Offset = &offset
	return lpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lpq *LikeProtocolQuery) Unique(unique bool) *LikeProtocolQuery {
	lpq.ctx.Unique = &unique
	return lpq
}

// Order specifies how the records should be ordered.
func (lpq *LikeProtocolQuery) Order(o ...likeprotocol.OrderOption) *LikeProtocolQuery {
	lpq.order = append(lpq.order, o...)
	return lpq
}

// First returns the first LikeProtocol entity from the query.
// Returns a *NotFoundError when no LikeProtocol was found.
func (lpq *LikeProtocolQuery) First(ctx context.Context) (*LikeProtocol, error) {
	nodes, err := lpq.Limit(1).All(setContextOp(ctx, lpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{likeprotocol.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lpq *LikeProtocolQuery) FirstX(ctx context.Context) *LikeProtocol {
	node, err := lpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LikeProtocol ID from the query.
// Returns a *NotFoundError when no LikeProtocol ID was found.
func (lpq *LikeProtocolQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lpq.Limit(1).IDs(setContextOp(ctx, lpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{likeprotocol.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lpq *LikeProtocolQuery) FirstIDX(ctx context.Context) int {
	id, err := lpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LikeProtocol entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LikeProtocol entity is found.
// Returns a *NotFoundError when no LikeProtocol entities are found.
func (lpq *LikeProtocolQuery) Only(ctx context.Context) (*LikeProtocol, error) {
	nodes, err := lpq.Limit(2).All(setContextOp(ctx, lpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{likeprotocol.Label}
	default:
		return nil, &NotSingularError{likeprotocol.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lpq *LikeProtocolQuery) OnlyX(ctx context.Context) *LikeProtocol {
	node, err := lpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LikeProtocol ID in the query.
// Returns a *NotSingularError when more than one LikeProtocol ID is found.
// Returns a *NotFoundError when no entities are found.
func (lpq *LikeProtocolQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lpq.Limit(2).IDs(setContextOp(ctx, lpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{likeprotocol.Label}
	default:
		err = &NotSingularError{likeprotocol.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lpq *LikeProtocolQuery) OnlyIDX(ctx context.Context) int {
	id, err := lpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LikeProtocols.
func (lpq *LikeProtocolQuery) All(ctx context.Context) ([]*LikeProtocol, error) {
	ctx = setContextOp(ctx, lpq.ctx, ent.OpQueryAll)
	if err := lpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LikeProtocol, *LikeProtocolQuery]()
	return withInterceptors[[]*LikeProtocol](ctx, lpq, qr, lpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lpq *LikeProtocolQuery) AllX(ctx context.Context) []*LikeProtocol {
	nodes, err := lpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LikeProtocol IDs.
func (lpq *LikeProtocolQuery) IDs(ctx context.Context) (ids []int, err error) {
	if lpq.ctx.Unique == nil && lpq.path != nil {
		lpq.Unique(true)
	}
	ctx = setContextOp(ctx, lpq.ctx, ent.OpQueryIDs)
	if err = lpq.Select(likeprotocol.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lpq *LikeProtocolQuery) IDsX(ctx context.Context) []int {
	ids, err := lpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lpq *LikeProtocolQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lpq.ctx, ent.OpQueryCount)
	if err := lpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lpq, querierCount[*LikeProtocolQuery](), lpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lpq *LikeProtocolQuery) CountX(ctx context.Context) int {
	count, err := lpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lpq *LikeProtocolQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lpq.ctx, ent.OpQueryExist)
	switch _, err := lpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lpq *LikeProtocolQuery) ExistX(ctx context.Context) bool {
	exist, err := lpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LikeProtocolQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lpq *LikeProtocolQuery) Clone() *LikeProtocolQuery {
	if lpq == nil {
		return nil
	}
	return &LikeProtocolQuery{
		config:     lpq.config,
		ctx:        lpq.ctx.Clone(),
		order:      append([]likeprotocol.OrderOption{}, lpq.order...),
		inters:     append([]Interceptor{}, lpq.inters...),
		predicates: append([]predicate.LikeProtocol{}, lpq.predicates...),
		// clone intermediate query.
		sql:  lpq.sql.Clone(),
		path: lpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Address string `json:"address,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LikeProtocol.Query().
//		GroupBy(likeprotocol.FieldAddress).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lpq *LikeProtocolQuery) GroupBy(field string, fields ...string) *LikeProtocolGroupBy {
	lpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LikeProtocolGroupBy{build: lpq}
	grbuild.flds = &lpq.ctx.Fields
	grbuild.label = likeprotocol.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Address string `json:"address,omitempty"`
//	}
//
//	client.LikeProtocol.Query().
//		Select(likeprotocol.FieldAddress).
//		Scan(ctx, &v)
func (lpq *LikeProtocolQuery) Select(fields ...string) *LikeProtocolSelect {
	lpq.ctx.Fields = append(lpq.ctx.Fields, fields...)
	sbuild := &LikeProtocolSelect{LikeProtocolQuery: lpq}
	sbuild.label = likeprotocol.Label
	sbuild.flds, sbuild.scan = &lpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LikeProtocolSelect configured with the given aggregations.
func (lpq *LikeProtocolQuery) Aggregate(fns ...AggregateFunc) *LikeProtocolSelect {
	return lpq.Select().Aggregate(fns...)
}

func (lpq *LikeProtocolQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lpq); err != nil {
				return err
			}
		}
	}
	for _, f := range lpq.ctx.Fields {
		if !likeprotocol.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lpq.path != nil {
		prev, err := lpq.path(ctx)
		if err != nil {
			return err
		}
		lpq.sql = prev
	}
	return nil
}

func (lpq *LikeProtocolQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LikeProtocol, error) {
	var (
		nodes = []*LikeProtocol{}
		_spec = lpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LikeProtocol).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LikeProtocol{config: lpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (lpq *LikeProtocolQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lpq.querySpec()
	_spec.Node.Columns = lpq.ctx.Fields
	if len(lpq.ctx.Fields) > 0 {
		_spec.Unique = lpq.ctx.Unique != nil && *lpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lpq.driver, _spec)
}

func (lpq *LikeProtocolQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(likeprotocol.Table, likeprotocol.Columns, sqlgraph.NewFieldSpec(likeprotocol.FieldID, field.TypeInt))
	_spec.From = lpq.sql
	if unique := lpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lpq.path != nil {
		_spec.Unique = true
	}
	if fields := lpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, likeprotocol.FieldID)
		for i := range fields {
			if fields[i] != likeprotocol.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lpq *LikeProtocolQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lpq.driver.Dialect())
	t1 := builder.Table(likeprotocol.Table)
	columns := lpq.ctx.Fields
	if len(columns) == 0 {
		columns = likeprotocol.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lpq.sql != nil {
		selector = lpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lpq.ctx.Unique != nil && *lpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lpq.predicates {
		p(selector)
	}
	for _, p := range lpq.order {
		p(selector)
	}
	if offset := lpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LikeProtocolGroupBy is the group-by builder for LikeProtocol entities.
type LikeProtocolGroupBy struct {
	selector
	build *LikeProtocolQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lpgb *LikeProtocolGroupBy) Aggregate(fns ...AggregateFunc) *LikeProtocolGroupBy {
	lpgb.fns = append(lpgb.fns, fns...)
	return lpgb
}

// Scan applies the selector query and scans the result into the given value.
func (lpgb *LikeProtocolGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lpgb.build.ctx, ent.OpQueryGroupBy)
	if err := lpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LikeProtocolQuery, *LikeProtocolGroupBy](ctx, lpgb.build, lpgb, lpgb.build.inters, v)
}

func (lpgb *LikeProtocolGroupBy) sqlScan(ctx context.Context, root *LikeProtocolQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lpgb.fns))
	for _, fn := range lpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lpgb.flds)+len(lpgb.fns))
		for _, f := range *lpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LikeProtocolSelect is the builder for selecting fields of LikeProtocol entities.
type LikeProtocolSelect struct {
	*LikeProtocolQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lps *LikeProtocolSelect) Aggregate(fns ...AggregateFunc) *LikeProtocolSelect {
	lps.fns = append(lps.fns, fns...)
	return lps
}

// Scan applies the selector query and scans the result into the given value.
func (lps *LikeProtocolSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lps.ctx, ent.OpQuerySelect)
	if err := lps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LikeProtocolQuery, *LikeProtocolSelect](ctx, lps.LikeProtocolQuery, lps, lps.inters, v)
}

func (lps *LikeProtocolSelect) sqlScan(ctx context.Context, root *LikeProtocolQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lps.fns))
	for _, fn := range lps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
